/* ButtonComponent JavaScript — demonstrates co-located JS auto-discovery.
 *
 * The gallery finds this file automatically because it sits next to
 * button.py and shares the same base name.  It is included in every
 * canvas iframe that renders a ButtonComponent.
 */
(function () {
  "use strict";
  // No-op placeholder — a real project would add click analytics,
  // ripple effects, or accessibility enhancements here.
})();
